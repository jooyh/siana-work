<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="CommonService">

	<select id="selectCommCdList" parameterType="Map" resultType="Map">
		SELECT /*[CommonService.selectCommCdList] 공통코드 조*/
				 COMM_CD AS commCd
				,COMM_NM AS commNm
				,GRP_CD  AS grpCd
		  FROM  API_COMM_CD
		 WHERE  GRP_CD = #{grpCd}
	</select>

	<insert id="insertFile" parameterType="Map" >
		INSERT /*[CommonService.insertFile] 파일 정보 등*/
		  INTO TMP_ATCH_FILE (
			 bbs_id
			,file_snm
			,file_onm
			,file_path
			,reg_id
			,upd_id
			,reg_dtm
			,upd_dtm
		  ) VALUES (
		  	 #{bbsId}
		  	,#{sNm}
		  	,#{oNm}
		  	,#{path}
		  	,#{session.userId}
		  	,#{session.userId}
		  	,NOW()
		  	,NOW()
		  )
	</insert>

	<select id="selectFile" parameterType="Map" resultType="Map">
		SELECT /*[CommonService.selectFile] 파일 정보 조회*/
				FILE_SNM  AS fileSnm
			 ,  FILE_ONM  AS fileOnm
			 ,  FILE_PATH AS filePath
		  FROM  TMP_ATCH_FILE
		 WHERE  BBS_ID = #{bbsId}
	</select>
</mapper>